[create.adaptSummary]
This response must include a top-level JSON field `adaptSummary` (string, required on adapt).

Write 2–4 short sentences for the person reading the recipe. Say what you changed, why it satisfies the `change` line and any `goal`, and any tradeoffs you made to stay within the system rules (pint fill, sugar grams, loaf weight, servings, etc.).

Do not repeat the full recipe in `adaptSummary`. Do not put this explanation in `notes` — keep `notes` for recipe tips only, or null.
